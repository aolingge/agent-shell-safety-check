Scope: workspace only. Run dry-run first. Destructive commands such as rm -rf or git reset --hard require confirmation. Verify with npm test.

Operational note: run this in CI with --markdown and --sarif.
